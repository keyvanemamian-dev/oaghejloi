timerobot.com`، ثبت‌نام کن**

**۲. `+ Add New Monitor`**

**۳. تنظیمات:**

| فیلد | مقدار |
|------|-------|
| Monitor Type | `HTTP(s)` |
| Friendly Name | `Bale Bot` |
| URL | آدرسی که Render داد |
| Interval | `5 minutes` |

**۴. `Create Monitor`**

---

## ✅ تموم!

**حالا رباتت ۲۴ ساعته کار می‌کنه!**

**لینک رباتت رو بده به دوستات:** `https://ble.ir/یوزرنیم_رباتت`

---

## 🔒 نکات امنیتی

- **توکن رو به کسی نده** (حتی توی چت)
- اگه توکن لو رفت → `Revoke Token` توی بات‌فادر
- توکن جدید رو توی GitHub عوض کن → Render خودش دوباره build می‌کنه

---

## 🐛 اگه به مشکل خوردی

**متن خطا رو بفرست تا کمکت کنم:**
- توی IDLE → خطای پایتون
- توی Render → تب `Logs`
- توی بله → ربات جواب نمیده

---

**این راهنما رو کپی کن و بده به هر کسی که می‌خوای!** 😊

**اگه سوالی داری، بپرس!** 🚀
import telebot
from telebot import apihelper

apihelper.API_URL = "https://tapi.bale.ai/bot{0}/{1}"

TOKEN = 1273875466:johT6Q_58Z7Gus9iZw8Vzgd0AdV58JpzHX0
ADMIN_ID = 2020589750
bot = telebot.TeleBot(TOKEN)

messages = {}

@bot.message_handler(commands=['start'])
def start(message):
    bot.reply_to(message, "سلام! پیام ناشناس بفرست، به دست صاحب ربات می‌رسه 😊")

@bot.message_handler(func=lambda m: True)
def anonymous(message):
    user_id = message.from_user.id
    text = message.text

    if user_id == ADMIN_ID:
        if message.reply_to_message:
            replied_id = message.reply_to_message.message_id
            target = messages.get(replied_id)
            if target:
                bot.send_message(target, f"💬 جواب:\n{text}")
                bot.reply_to(message, "✅ جواب فرستاده شد")
            else:
                bot.reply_to(message, "❌ پیدا نشد (آیدی پیام: {})".format(replied_id))
        else:
            bot.reply_to(message, "برای جواب، روی پیام ریپلای کن")
    else:
        sent = bot.send_message(ADMIN_ID, f"📩 پیام ناشناس:\n\n{text}")
        messages[sent.message_id] = user_id
        print("ذخیره شد: پیام {} ← کاربر {}".format(sent.message_id, user_id))
        bot.reply_to(message, "✅ پیامت رسید!")

print("ربات روشن شد...")
bot.infinity_polling()
